  
  ** two min **
  
  return (r+" IST "+"/"+s + " #" +a  + " *" + lst1)
  r : current time
  s : current value
  a : predicted value
  lst1 : graph value in increasing time format where
  					x: unix timestamp
  					y: current price
  					z: predicted price
  					
  					
  ** one day and one week **
  
  return (t.strftime("%Y-%m-%d %H:%M:%S")+ " IST /" + cur+ " #" + val + " 1" + " *"+ lst2  )	
  t.strftime("%Y-%m-%d %H:%M:%S") : current time
  cur : current value
  val : predicted value
  0 or 1 : downtrend or uptrend
  lst2 : graph value in increasing time format where
  					x: unix timestamp
  					y: current price
  	last x:unix timestamp
  	last y:predicted price 			
